<style global>
  @tailwind base;
  @tailwind components;
  .calInput {
    @apply appearance-none block w-full bg-gray-200 text-gray-700 rounded
        py-3 px-4 mb-3 leading-tight;
  }
  .calInput:focus {
    @apply outline-none bg-white;
  }
  .calSelect {
    @apply appearance-none inline w-1/3 bg-gray-200 text-gray-700 rounded
        py-3 px-4 mb-3 leading-tight;
  }
  @tailwind utilities;
</style>
